{# empty Twig template #}
<h1>Listado de Articulos</h1>
<table border="1">
    <tr>
        <th>ID</th>
        <th>Titulo</th>
        <th>Fecha de Creacion</th>
        <th>Fecha de Modificacion</th>
    </tr>
    {% for articulo in articulos %}
    <tr>
        <td>{{ articulo.id }}</td>
        <td>{{ articulo.title }}</td>
        <td>{{ articulo.created | date('d-m-Y') }}</td>
        <td>{{ articulo.updated | date('d-m-Y') }}</td>
    </tr>
    {% endfor %}
</table>