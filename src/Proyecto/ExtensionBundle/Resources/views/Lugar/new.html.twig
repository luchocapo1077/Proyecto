{% extends "ProyectoExtensionBundle::layout.html.twig" %}

{% block title %}Lugares{% endblock %}

{% block body %}
<h1>Crear Lugar</h1>

<form action="{{ path('lugar_create') }}" method="post" {{ form_enctype(form) }}>
    {{ form_widget(form) }}
    <p>
        <button type="submit">Guardar</button>
    </p>
</form>

<ul class="record_actions">
    <li>
        <a href="{{ path('lugar') }}">
            Volver al listado
        </a>
    </li>
</ul>
{% endblock %}
