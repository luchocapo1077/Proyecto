{% extends "ProyectoExtensionBundle::layout.html.twig" %}

{% block title %}Periodos{% endblock %}

{% block body %}
h1>Editar Periodo</h1>

<form action="{{ path('periodo_update', { 'id': entity.id }) }}" method="post" {{ form_enctype(edit_form) }}>
    {{ form_widget(edit_form) }}
    <p>
        <button type="submit">Guardar</button>
    </p>
</form>

<ul class="record_actions">
    <li>
        <a href="{{ path('periodo') }}">
            Volver al listado
        </a>
    </li>
    <li>
        <form action="{{ path('periodo_delete', { 'id': entity.id }) }}" method="post">
            {{ form_widget(delete_form) }}
            <button type="submit">Borrar</button>
        </form>
    </li>
</ul>
{% endblock %}
