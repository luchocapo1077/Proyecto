{% block title "Filtrando Extensiones " %}

{% block content %}
    {#- comment -#}
    {#{ google_map_css(map) }#}
    <h1>Extensiones</h1>  
    {{ google_map_container(map) }}
    {{ google_map_js(map) }}
    
    <form action="{{ path('proyecto_mapFilter')}}" method="post" {{ form_enctype(filter_form) }}>
    {{ form_widget(filter_form) }}
    <p>
        <button type="submit">Filtrar</button>
    </p>
    </form>    
    
    
{% endblock %}

{% set code = code(_self) %}
