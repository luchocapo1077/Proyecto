{% extends "AcmeDemoBundle::layout.html.twig" %}

{% block title "Hello " %}

{% block content %}
    {#- comment -#}
    {#{ google_map_css(map) }#}
    <h1>Google Maps</h1>  
    {{ google_map_container(map) }}
    {{ google_map_js(map) }} 
    <ul id="demo-list">
        <li><a href="{{ path('_demo_mapFilter', {'map': 'map'  }) }}">Agregar Marker</a></li>        
    </ul>
{% endblock %}

{% set code = code(_self) %}
